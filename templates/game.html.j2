<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ game.title or slug }}</title>
  <link rel="stylesheet" href="../../assets/game.css">
  <link rel="icon" type="image/png" href="../../assets/utc-olp.png">
</head>
<body>
  
  <main>
    {% include 'sprig_info.html.j2' %}
    <canvas id="canvas" width="800" height="600" tabindex="0"></canvas>
    <aside id="game_info">
      <h3  id="title">{{ game.title or filename }}</h3>
      <p class="author">by <span id="author">{{ game.author or 'Unknown' }}</span></p>
      <p id="description">{{ game.description or '-' }}</p>
    </aside>
  </main>
  <script type="module">
    import { webEngine } from "https://esm.sh/sprig@1/web";

    const canvas = document.getElementById("canvas");
    fetch(`../{{ slug }}.js`)
      .then(r => r.text())
      .then(code => {
        const game = webEngine(canvas);
        const fn = new Function(...Object.keys(game.api), code);
        fn(...Object.values(game.api));
        canvas.focus();
    });

    document.addEventListener('click', () => canvas.focus());
  </script>
</body>
</html>